--[[

level 0
- aka 'Player'
- Commands that enable players (i.e. nonadmins) to access basics pages
or view low-level information or provide very basic self utility 

--]]




local main = require(game.HDAdmin)
return {
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(self, caller, args)
			
		end,
		revoke = function(self, caller, args)
			
		end,
	};
	
	
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(self, caller, args)
			
		end,
		revoke = function(self, caller, args)
			
		end;
	};
	
	
	
	-----------------------------------
	
};