--[[

level 2
- aka 'Mod'
- Primarily user enhancing commands, combined with some server-impacting commands,
that at most only cause mild abuse

--]]



local main = require(game.HDAdmin)
return {
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(this, caller, args)
			
		end,
		revoke = function(this, caller, args)
			
		end,
	};
	
	
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(this, caller, args)
			
		end,
		revoke = function(this, caller, args)
			
		end,
	};
	
	
	
	-----------------------------------
	
};