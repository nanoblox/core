--[[

level 3
- aka 'Admin' and 'HeadAdmin' (the key difference is HeadAdmins by default
can execute commands globally
- A collection of moderation-based, abusive and fun commands

--]]



local main = require(game.Nanoblox)
return {
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(this, caller, args)
			
		end,
		revoke = function(this, caller, args)
			
		end,
	};
	
	
	
	-----------------------------------
	{
		name = "",
		aliases	= {},
		prefixes = {},
		args = {"Player"},
		invoke = function(this, caller, args)
			
		end,
		revoke = function(this, caller, args)
			
		end,
	};
	
	
	
	-----------------------------------
	
};